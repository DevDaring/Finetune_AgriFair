# Pinned global requirements for the AgriFair GRAFT study.
# Install with:  pip install -r requirements_global.txt
# Flash-attention is installed separately from a pre-built wheel by
# GPU_Run/common/flash_attn_setup.py and is never compiled from source; sdpa is the
# fallback and the choice is recorded per model in every results CSV.

torch>=2.3
transformers>=4.51          # Qwen3 and Gemma 3 support
jinja2>=3.1.0               # REQUIRED: transformers treats it as optional, but apply_chat_template
                            # raises ImportError below 3.1 and the study would then send every
                            # prompt as raw text with no chat template and no BOS token
peft>=0.15                  # rank_pattern and alpha_pattern regex matching
datasets>=2.20
accelerate>=0.33
bitsandbytes>=0.43          # 4-bit NF4 QLoRA arm (CUDA only)
safetensors>=0.4            # adapter weight reload on resume
huggingface_hub>=0.24
numpy>=1.26
scipy>=1.11                 # bootstrap, Spearman, exact binomial for McNemar
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
sacrebleu>=2.4
rouge-score>=0.1.2
requests>=2.31
boto3>=1.35                 # AWS Bedrock, for the frozen frontier panel only

# Optional. sentence-transformers is used only when MULTILINGUAL_EMBED_MODEL is set, for
# the AgriAdvice drift embedding; without it the drift falls back to TF-IDF cosine and the
# results CSV records which backend produced each number.
sentence-transformers>=2.7
